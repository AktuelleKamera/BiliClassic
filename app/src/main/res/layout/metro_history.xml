<?xml version="1.0" encoding="utf-8"?>
<!--
  Metro 风格历史记录页（MetroHome 内的 Fragment，与推荐/个人中心同构）：
  大字粉色标题 + 下拉刷新 + 透明列表（条目用共用的 item_metro_video_row）。
  转门翻入/翻出动画由 MetroHomeActivity 的页面容器统一驱动。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- 页标题（每页自带） -->
        <TextView
            android:id="@+id/page_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="24dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="10dp"
            android:text="@string/history_record"
            android:textSize="44sp"
            android:textColor="#D86DA5"/>

        <!-- 下拉刷新：包裹历史列表 -->
        <android.support.v4.widget.SwipeRefreshLayout
            android:id="@+id/swipe_refresh_metro"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <ListView
                android:id="@+id/history_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="16dp"
                android:paddingRight="16dp"
                android:paddingBottom="24dp"
                android:divider="@android:color/transparent"
                android:dividerHeight="0dp"
                android:listSelector="@android:color/transparent"
                android:cacheColorHint="@android:color/transparent"
                android:scrollbars="none"
                android:clipToPadding="false"
                android:visibility="gone" />

        </android.support.v4.widget.SwipeRefreshLayout>

    </LinearLayout>

    <!-- 加载/状态提示：在整屏居中显示（加载中/为空/失败，点击可重试） -->
    <TextView
        android:id="@+id/tv_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="正在加载…"
        android:textSize="16sp"
        android:textColor="#999999" />

</FrameLayout>
