<?xml version="1.0" encoding="utf-8"?>
<!--
  Metro 风格通用视频行 item：历史记录 / 收藏视频共用。
  透明背景（直接叠在 Metro 页面上），包含两套副标题 ID：
    - 历史记录用 title / up_name / progress
    - 收藏视频用 title / up_name / view
  各 Adapter 只绑定并填充自己的字段，另一组留空（空文本不占高度）。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingLeft="6dp"
    android:paddingRight="6dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:background="@android:color/transparent">

    <!-- 封面 -->
    <FrameLayout
        android:id="@+id/cover_container"
        android:layout_width="112dp"
        android:layout_height="66dp"
        android:background="@drawable/bili_thumb_boarder"
        android:padding="3dp">

        <ImageView
            android:id="@+id/cover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/bili_default_image_tv_with_bg"
            android:contentDescription="@null" />

    </FrameLayout>

    <!-- 文案区 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginLeft="12dp"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="#333333"
            android:maxLines="2"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/up_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textSize="12sp"
            android:textColor="#999999"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- 历史记录：观看进度 -->
        <TextView
            android:id="@+id/progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textSize="11sp"
            android:textColor="#D86DA5"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- 收藏视频：观看数 -->
        <TextView
            android:id="@+id/view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textSize="11sp"
            android:textColor="#D86DA5"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

</LinearLayout>
