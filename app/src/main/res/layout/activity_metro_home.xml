<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- 两个页面叠放（都满屏），从而实现真正的"书页转门"重叠翻转 -->
    <FrameLayout
        android:id="@+id/page_holder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- 主页（封面）：标题 + 菜单作为一个整体 -->
        <tv.biliclassic.metro.TurnLayout
            android:id="@+id/home_page"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:clipChildren="false"
            android:clipToPadding="false">

            <TextView
                android:id="@+id/title_home"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="24dp"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="10dp"
                android:text="@string/metro_home_title"
                android:textSize="44sp"
                android:textColor="#D86DA5"/>

            <ScrollView
                android:id="@+id/menu_scroll"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_marginLeft="40dp"
                android:layout_marginRight="16dp"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="16dp"
                android:scrollbars="none">

                <LinearLayout
                    android:id="@+id/metro_menu"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

            </ScrollView>

        </tv.biliclassic.metro.TurnLayout>

        <!-- 详情页（封底）：内容由各 Fragment 提供（含各自标题）；默认隐藏 -->
        <tv.biliclassic.metro.TurnLayout
            android:id="@+id/recommend_page"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:visibility="invisible">

            <FrameLayout
                android:id="@+id/metro_fragment_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipChildren="false"
                android:clipToPadding="false" />

        </tv.biliclassic.metro.TurnLayout>

    </FrameLayout>

    <!-- 右下角小电视水印（bilibili_tv） -->
    <ImageView
        android:id="@+id/tv_watermark"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_gravity="bottom|end"
        android:layout_marginRight="-40dp"
        android:layout_marginBottom="-40dp"
        android:src="@drawable/bilibili_tv"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:contentDescription="@null" />

</FrameLayout>
