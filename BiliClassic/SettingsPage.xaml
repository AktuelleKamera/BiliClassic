<phone:PhoneApplicationPage
    x:Class="BiliClassic.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!-- 转场 -->
    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>

    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,16">
            <TextBlock Text="哔哩经典" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="设置" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="12,0,12,0">
            <CheckBox x:Name="OfflinePlaybackCheck" Content="离线播放"
                      Checked="OfflinePlayback_Changed"
                      Unchecked="OfflinePlayback_Changed"/>
            <TextBlock Margin="12,0,0,0" TextWrapping="Wrap"
                       Style="{StaticResource PhoneTextSubtleStyle}"
                       Text="打开后将不再在线播放视频，只要还能播放就建议不要开启的说！"/>

            <CheckBox x:Name="DanmakuCheck" Content="弹幕"
                      Margin="0,20,0,0"
                      Checked="Danmaku_Changed"
                      Unchecked="Danmaku_Changed"/>
            <CheckBox x:Name="AutoCheckUpdateCheck" Content="自动检查更新"
                      Margin="0,12,0,0"
                      Checked="AutoCheckUpdate_Changed"
                      Unchecked="AutoCheckUpdate_Changed"/>
            <Button x:Name="CheckUpdateButton" Content="检查更新"
                    Margin="0,4,0,0" Click="CheckUpdate_Click"/>

            <!-- 清除缓存 -->
            <Button x:Name="ClearCacheButton" Content="清除离线缓存"
                    Margin="0,28,0,0" Click="ClearCache_Click"/>
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" Margin="0,8,0,0"
                       Style="{StaticResource PhoneTextSubtleStyle}"/>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>
