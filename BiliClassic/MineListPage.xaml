<phone:PhoneApplicationPage
    x:Class="BiliClassic.MineListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.Resources>

        <!-- 视频卡片 -->
        <DataTemplate x:Key="VideoCard">
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="112" Height="70"
                        Margin="0,0,12,0" Background="#22FFFFFF"
                        BorderBrush="{StaticResource PhoneSubtleBrush}" BorderThickness="2">
                    <Border.Clip>
                        <RectangleGeometry Rect="0,0,112,70"/>
                    </Border.Clip>
                    <!-- 默认占位图 -->
                    <Grid>
                        <Image Stretch="UniformToFill"
                               Source="/Res/bili_default_image_tv_with_bg.png"/>
                        <Image Stretch="UniformToFill" Source="{Binding Cover}"/>
                        <!-- 私密收藏夹角标 -->
                        <Border HorizontalAlignment="Right" VerticalAlignment="Top"
                                Background="#99A0A0A0" Padding="6,1"
                                Visibility="{Binding BadgeVisibility}">
                            <TextBlock Text="{Binding Badge}" FontSize="16" Foreground="White"/>
                        </Border>
                    </Grid>
                </Border>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap" MaxHeight="72"
                               Margin="0"
                               Style="{StaticResource PhoneTextNormalStyle}"/>
                    <TextBlock Text="{Binding InfoLine}" TextWrapping="Wrap" MaxHeight="40"
                               Margin="0,2,0,0"
                               Style="{StaticResource PhoneTextSubtleStyle}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- 用户卡片 -->
        <DataTemplate x:Key="UserCard">
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="70" Height="70"
                        VerticalAlignment="Top"
                        Margin="0,2,12,0" Background="#22FFFFFF"
                        BorderBrush="{StaticResource PhoneSubtleBrush}" BorderThickness="2">
                    <Border.Clip>
                        <RectangleGeometry Rect="0,0,70,70"/>
                    </Border.Clip>
                    <!-- 显示默认头像 -->
                    <Grid>
                        <Image Stretch="UniformToFill"
                               Source="/Res/bili_default_avatar.png"/>
                        <Image Stretch="UniformToFill" Source="{Binding Avatar}"/>
                    </Grid>
                </Border>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Name}" TextWrapping="Wrap" MaxHeight="44"
                               Margin="0"
                               Style="{StaticResource PhoneTextNormalStyle}"/>
                    <TextBlock Text="{Binding Sign}" TextWrapping="Wrap" MaxHeight="40"
                               Margin="0,2,0,0"
                               Style="{StaticResource PhoneTextSubtleStyle}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

    </phone:PhoneApplicationPage.Resources>

    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>

    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,16">
            <TextBlock Text="哔哩经典" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock x:Name="PageTitle" Text="我的" Margin="9,-7,0,0"
                       Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="12,0,12,0">
            <ListBox x:Name="ResultList" ItemTemplate="{StaticResource VideoCard}"
                     SelectionChanged="ResultList_SelectionChanged"/>
        </Grid>

        <StackPanel Grid.Row="2" Margin="12,4,12,8">
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" Text="正在加载…"
                       Style="{StaticResource PhoneTextSubtleStyle}"/>
            <Button x:Name="MoreButton" Content="加载更多" Visibility="Collapsed"
                    Click="MoreButton_Click"/>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>
