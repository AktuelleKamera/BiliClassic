<phone:PhoneApplicationPage
    x:Class="BiliClassic.VideoPlayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Landscape"
    shell:SystemTray.IsVisible="False">

    <!-- 转场 -->
    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>

    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <!-- 黑底 -->
    <Grid x:Name="LayoutRoot" Background="Black">
        <!-- DASH音频轨：和视频轨配成一对播，放视频的那条在它上面 -->
        <MediaElement x:Name="AudioPlayer" Stretch="Uniform" AutoPlay="False"
                      MediaOpened="AudioPlayer_MediaOpened"
                      MediaFailed="AudioPlayer_MediaFailed"/>

        <MediaElement x:Name="Player" Stretch="Uniform" AutoPlay="False"
                      MediaOpened="Player_MediaOpened"
                      MediaFailed="Player_MediaFailed"
                      CurrentStateChanged="Player_CurrentStateChanged"/>

        <!-- 弹幕层 -->
        <Canvas x:Name="DanmakuLayer" IsHitTestVisible="False"/>

        <!-- 透明层 点击 -->
        <Grid x:Name="TapLayer" Background="Transparent"
              MouseLeftButtonUp="TapLayer_MouseLeftButtonUp"/>

        <TextBlock x:Name="TitleText" VerticalAlignment="Top" Margin="12,12,12,0"
                   Foreground="White" TextWrapping="Wrap" MaxHeight="60"/>

        <!-- 底部 -->
        <StackPanel VerticalAlignment="Bottom">
            <!-- 清晰度选单：点右下角按钮才展开 -->
            <Border x:Name="QualityPanel" Visibility="Collapsed"
                    HorizontalAlignment="Right" Margin="0,0,4,0"
                    Background="#EE101010" Padding="8,6">
                <StackPanel x:Name="QualityList"/>
            </Border>

            <!-- 播放控制条 -->
            <Border x:Name="ControlPanel" Background="#CC000000" Padding="12,4">
                <StackPanel>
                    <Slider x:Name="SeekBar" Minimum="0" Maximum="1" Value="0"
                            ValueChanged="SeekBar_ValueChanged"/>
                    <!-- 负上边距盖掉Slider模板自带的空白 -->
                    <Grid Margin="0,-28,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!-- 图标用白色蒙版，原图的颜色不影响 -->
                        <Button x:Name="PlayPauseButton" Grid.Column="0"
                                MinWidth="0" Padding="8,4"
                                Background="Transparent" BorderThickness="0"
                                Click="PlayPauseButton_Click">
                            <Rectangle Width="40" Height="40" Fill="White">
                                <Rectangle.OpacityMask>
                                    <ImageBrush x:Name="PlayPauseMask"
                                                ImageSource="/Res/Icon/play.png"/>
                                </Rectangle.OpacityMask>
                            </Rectangle>
                        </Button>
                        <TextBlock x:Name="PositionText" Grid.Column="1"
                                   Foreground="White" VerticalAlignment="Center"
                                   TextAlignment="Center" Text="00:00 / --:--"/>
                        <!-- 实际走的是哪条路，开播前为空 -->
                        <TextBlock x:Name="TransportText" Grid.Column="2" Text=""
                                   Foreground="White" VerticalAlignment="Center"
                                   FontSize="16" Margin="0,0,8,0"/>
                        <Button x:Name="DanmakuButton" Grid.Column="3"
                                MinWidth="0" Padding="8,4"
                                Background="Transparent" BorderThickness="0"
                                Click="DanmakuButton_Click">
                            <Rectangle x:Name="DanmakuIcon" Width="40" Height="40" Fill="White">
                                <Rectangle.OpacityMask>
                                    <ImageBrush x:Name="DanmakuMask"
                                                ImageSource="/Res/Icon/danmuon.png"/>
                                </Rectangle.OpacityMask>
                            </Rectangle>
                        </Button>
                        <!-- 右下角：点开清晰度选单 -->
                        <Button x:Name="QualityButton" Grid.Column="4" Content="720P"
                                MinWidth="0" Padding="12,2"
                                Click="QualityButton_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border x:Name="StatusPanel" Background="#99000000" Padding="12,8">
                <TextBlock x:Name="StatusText" Foreground="White" TextWrapping="Wrap"
                           Text="正在获取视频信息…"/>
            </Border>
        </StackPanel>

        <!-- 仿安卓版小电视动画~ -->
        <Grid x:Name="LoadingPanel" Background="#FFBABABA">
            <Image x:Name="LoadingTv" Width="120" Height="120"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock x:Name="LoadingText" Margin="12,0,12,20"
                       HorizontalAlignment="Left" VerticalAlignment="Bottom"
                       Foreground="#FF616161" FontSize="18"
                       Text="正在获取视频信息…"/>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>
