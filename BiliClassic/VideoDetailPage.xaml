<phone:PhoneApplicationPage
    x:Class="BiliClassic.VideoDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    shell:SystemTray.IsVisible="True">

    <!-- 页面转场动画 -->
    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>

    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 页标题交给Pivot的标签 -->
        <StackPanel Grid.Row="0" Margin="12,17,0,0">
            <TextBlock Text="哔哩经典" Style="{StaticResource PhoneTextNormalStyle}"/>
        </StackPanel>

        <controls:Pivot x:Name="DetailPivot" Grid.Row="1"
                        SelectionChanged="DetailPivot_SelectionChanged">

            <controls:PivotItem Header="详情">
                <ScrollViewer Margin="12,0,12,0">
                    <StackPanel Margin="0,0,0,24">
                        <!-- 封面 -->
                        <Border Background="#22FFFFFF" BorderBrush="{StaticResource PhoneSubtleBrush}"
                                BorderThickness="2" HorizontalAlignment="Center">
                            <Image x:Name="CoverImage" Width="360" Height="203" Stretch="UniformToFill"/>
                        </Border>

                        <TextBlock x:Name="TitleText" TextWrapping="Wrap" Margin="0,12,0,0"
                                   FontSize="26"/>

                        <!-- 迫真播放按钮 -->
                        <Button x:Name="PlayButton" Content="播放视频"
                                Height="72" Margin="0,12,0,0"
                                Background="#D86DA5" Foreground="White"
                                BorderThickness="0"
                                Click="PlayButton_Click"/>

                        <TextBlock x:Name="AuthorText" TextWrapping="Wrap" Margin="0,6,0,0"
                                   Style="{StaticResource PhoneTextNormalStyle}"/>
                        <TextBlock x:Name="StatText" TextWrapping="Wrap" Margin="0,4,0,0"
                                   Style="{StaticResource PhoneTextSubtleStyle}"/>
                        <TextBlock x:Name="PubDateText" TextWrapping="Wrap" Margin="0,8,0,0"
                                   Style="{StaticResource PhoneTextSubtleStyle}"/>
                        <TextBlock x:Name="DescText" TextWrapping="Wrap" Margin="0,12,0,0"
                                   Style="{StaticResource PhoneTextSubtleStyle}"/>

                        <!-- 分P：单P视频整块隐藏 -->
                        <TextBlock x:Name="PartsHeader" Margin="0,20,0,6"
                                   Style="{StaticResource PhoneTextSubtleStyle}"
                                   Visibility="Collapsed"/>
                        <ListBox x:Name="PartsList" Visibility="Collapsed"
                                 SelectionChanged="PartsList_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock Text="{Binding DisplayTitle}" TextWrapping="Wrap"
                                                   Style="{StaticResource PhoneTextNormalStyle}"/>
                                        <TextBlock Text="{Binding DurationText}"
                                                   Style="{StaticResource PhoneTextSubtleStyle}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </ScrollViewer>
            </controls:PivotItem>

            <controls:PivotItem Header="评论">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox x:Name="CommentList" Grid.Row="0" Margin="12,0,12,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 头像走AvatarLoader，显式尺寸加Clip -->
                                    <!-- 顶对齐：默认居中的话和昵称那一行对不上 -->
                                    <!-- 文字自带12px左边距，这里不再加右边距 -->
                                    <Border Grid.Column="0" Width="56" Height="56"
                                            VerticalAlignment="Top"
                                            Margin="0,2,0,0"
                                            Background="#22FFFFFF"
                                            BorderBrush="{StaticResource PhoneSubtleBrush}"
                                            BorderThickness="2">
                                        <Border.Clip>
                                            <RectangleGeometry Rect="0,0,56,56"/>
                                        </Border.Clip>
                                        <Grid Width="50" Height="50">
                                            <Image Stretch="UniformToFill"
                                                   Source="/Res/bili_default_avatar.png"/>
                                            <Image Stretch="UniformToFill"
                                                   Source="{Binding Author.Avatar}"/>
                                        </Grid>
                                    </Border>

                                    <!-- PhoneText*Style自带12px左边距，这里逐个覆盖
                                         不然头像和文字之间会多出一段空白 -->
                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                        <TextBlock Text="{Binding Author.Name}" TextWrapping="Wrap"
                                                   Margin="0"
                                                   Style="{StaticResource PhoneTextNormalStyle}"/>
                                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap"
                                                   Margin="0,2,0,0"
                                                   Style="{StaticResource PhoneTextSubtleStyle}"/>
                                        <TextBlock Text="{Binding InfoLine}" Margin="0,4,0,0"
                                                   Style="{StaticResource PhoneTextSubtleStyle}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- 没有评论时的占位由代码创建，见ApplyComments -->

                    <StackPanel Grid.Row="1" Margin="12,4,12,8">
                        <TextBlock x:Name="CommentStatus" TextWrapping="Wrap" Text=""
                                   Style="{StaticResource PhoneTextSubtleStyle}"/>
                        <Button x:Name="MoreCommentButton" Content="加载更多"
                                Visibility="Collapsed" Click="MoreCommentButton_Click"/>
                    </StackPanel>
                </Grid>
            </controls:PivotItem>

        </controls:Pivot>

        <Border Grid.Row="2" Background="#33FFFFFF" Padding="12,10">
            <TextBlock x:Name="StatusText" Text="正在加载…" TextWrapping="Wrap"
                       Style="{StaticResource PhoneTextSubtleStyle}"/>
        </Border>
    </Grid>

</phone:PhoneApplicationPage>
